query chatroomType($accessToken:String!, $username:String){
    chatrooms (accessToken:$accessToken, username:$username){
        chatroomId
        userFrom{
            name
            kakaoID
            profile
        }
        userTo{
            name
            kakaoID
            profile
        }
    }
}