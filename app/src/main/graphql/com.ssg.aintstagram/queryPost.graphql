query postType($name:String, $accessToken:String!){
    posts (username: $name, accessToken: $accessToken){
        postId
        allowComment
        place
        textComment
        likeCount
        user{
            name
            profile
        }
    }
}
