query userType($name:String, $kakaoID:Int, $accessToken:String, $search:Int){
    users (username: $name, kakaoID: $kakaoID, accessToken: $accessToken, search: $search){
        name
        kakaoID
        textComment
        postCount
        followerCount
        followingCount
        profile
        isOpen
    }
}
