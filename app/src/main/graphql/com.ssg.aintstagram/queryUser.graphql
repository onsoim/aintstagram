query userType($name:String, $kakaoID:Int, $accessToken:String){
    users (username: $name, kakaoID: $kakaoID, accessToken: $accessToken){
        name
        kakaoID
        textComment
        postCount
        followerCount
        followingCount
        profile
        isOpen
    }
}
